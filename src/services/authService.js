// TODO: login(email, password) → POST /api/v1/login → retourne { token, user }
// TODO: register(name, email, password) → POST /api/v1/register
// TODO: logout() → POST /api/v1/logout

const authService = {};

export default authService;
