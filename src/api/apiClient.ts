// TODO: configurer baseURL depuis import.meta.env.VITE_API_URL
// TODO: ajouter interceptor Authorization Bearer (token depuis authStore)
// TODO: ajouter interceptor réponse 401 → authStore.logout()

const apiClient = {};

export default apiClient;
